% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapCodesToPhecodes.R
\name{mapCodesToPhecodes}
\alias{mapCodesToPhecodes}
\title{Map Medical Codes to Phecodes}
\usage{
mapCodesToPhecodes(
  input,
  vocabulary.map = NULL,
  rollup.map = NULL,
  make.distinct = TRUE
)
}
\arguments{
\item{input}{A data frame with at least two columns: \code{vocabulary_id}
(one of "ICD9CM", "ICD10CM", "OPCS4", "OPCS3", "READv2", "READv3",
"SNOMEDCT") and \code{code}
(the medical code as character). Additional columns (e.g., patient ID) are
preserved.}

\item{vocabulary.map}{The vocabulary-to-phecode mapping table. Defaults to
the package's \code{extended_phecode_map}.}

\item{rollup.map}{The phecode rollup map for hierarchical expansion. Defaults
to \code{extended_rollup_map}. Set to NULL to disable rollup.}

\item{make.distinct}{Logical. Remove duplicate rows? Default TRUE.}
}
\value{
A data frame with the original columns plus a \code{phecode} column,
  with \code{code} and \code{vocabulary_id} removed.
}
\description{
Maps ICD-9, ICD-10, OPCS-3, OPCS-4, Read v2, Read v3, and SNOMED-CT codes
to phecodes using the extended phecode mapping tables.
}
\examples{
\dontrun{
# Map a mix of ICD-10 and OPCS-4 codes
input <- data.frame(
  id = c(1, 1, 2, 2),
  vocabulary_id = c("ICD10CM", "OPCS4", "ICD10CM", "OPCS4"),
  code = c("N20.0", "M091", "K80.0", "J181"),
  stringsAsFactors = FALSE
)
mapped <- mapCodesToPhecodes(input)
}
}
